enum MyWorkmanagerStatic {
  periodic(
    "com.pravasta.a_restroApp",
    "com.pravasta.a_restroApp",
  );

  final String uniqueName;
  final String taskName;

  const MyWorkmanagerStatic(this.uniqueName, this.taskName);
}
